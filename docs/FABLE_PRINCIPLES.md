# FABLE_PRINCIPLES — 可复用立法（遇同类分叉先查此文件，不再送裁）

> 本地文档，勿提交公开仓库。

1. **「显示值 vs 真值」** → 默认真值赢：一切对外数字构建期从数据源计算，禁止存在第二份手写副本。
2. **「校验放哪层」** → 默认双层：入口（admin）拒绝 + 构建致命失败；静默兜底/静默覆盖一律否决，兜底只许做第三道保险。
3. **「哪层实现」** → 默认按 生成期 > Worker 运行时 > 客户端 JS 取最靠前可行层；同效果不下沉。
4. **「内容缺位」** → 默认降级下线该位而非占位：空态要设计，但空态不占主导航；真实内容到位才恢复。绝不占位假文案。
5. **「性能优化」** → 默认先删字节（拆包/静态化/去截图）再谈技术手段；判准只有两个——低端机首屏可交互、转化页 LCP。
